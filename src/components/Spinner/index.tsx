import { CircularProgress } from '@mui/material';
import { CustomBox } from './spinner.styled';

const Spinner = () => (
  <CustomBox>
    <CircularProgress />
  </CustomBox>
);

export default Spinner;
